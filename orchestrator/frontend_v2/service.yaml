apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: frontend-v2
spec:
  template:
    spec:
      containers:
      - image: placeholder-image
        ports:
        - containerPort: 8080
